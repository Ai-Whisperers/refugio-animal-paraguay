# RAP-200 References

- src/notifications/whatsapp_service.py — existing Twilio service (pattern reference)
- src/config.py — settings to extend
- src/notifications/meta_whatsapp_service.py — new file to create
- tests/unit/test_whatsapp_service.py — test pattern reference
- tests/unit/test_meta_whatsapp_service.py — new test file to create
