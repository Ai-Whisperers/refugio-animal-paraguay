# RAP-536 References

## Key files
- src/db/models/campaign.py — Campaign model (add rescuer_id)
- src/db/models/rescuer_profile.py — RescuerProfile (is_verified for auto-approval)
- src/api/rescuer_animals.py — Portal pattern to follow
- src/api/rescuer_profile.py — Public rescuer profile (campaigns GET endpoint)
- frontend/src/app/portal/rescuer/animals/ — Frontend portal pattern
- src/app.py — Router registration
