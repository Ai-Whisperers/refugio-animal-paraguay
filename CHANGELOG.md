# Changelog

All notable changes to Refugio Animal Paraguay will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added
- Initial `.claude/` AI development framework with rules, commands, skills, agents, and hooks
- Ticket workflow system (`tickets/` directory with structured plan/context/progress files)
- Architecture Decision Records structure (`docs/adr/`)
- `llms.txt` project specification for AI context loading
- Skills: `fastapi-patterns`, `payment-patterns`, `eu-donation-patterns`, `postgresql-patterns`, `python-patterns`, `rest-api-patterns`, `testing-patterns`, `paraguayan-animal-law`
- Commands: `/start-ticket`, `/complete-ticket`, `/update-progress`, `/switch-ticket`, `/execute-plan`, `/create-branch`, `/create-pr`, `/changelog`, `/pre-commit-check`, `/code-review`, `/refactor`, `/generate-missing-tests`, `/create-migration`, `/create-story`, `/create-readme`, `/domain-spec`, `/catchup`, `/adr`
- Project documentation: `docs/PROJECT-OVERVIEW.md`, `docs/DATA-MODEL.md`, `docs/COMPLIANCE.md`, `docs/USER-PERSONAS.md`, `docs/TECH-STACK.md`, `docs/ARCHITECTURE.md`, `docs/ROADMAP.md`, `docs/EPICS.md`
- ADR exemplar at `.claude/exemplars/adr/adr-good.md`

---

<!-- CHANGELOG maintained by /changelog command. Add entries under [Unreleased] during development. -->
